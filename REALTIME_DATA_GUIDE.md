# 实时数据功能使用指南

## 功能概述

投资跟踪系统现在支持从中国股票市场实时抓取数据，动态筛选符合条件的高股息股票和券商股票。

## 数据源

系统使用 **AKShare** 开源金融数据接口，特点：
- ✅ 完全免费，无需注册
- ✅ 支持A股实时数据
- ✅ 包含股息率、财务指标等
- ✅ 数据来源可靠

## 功能特性

### 1. 实时数据抓取

- **高股息股票筛选**
  - 自动筛选股息率 > 2% 的股票
  - 按股息率分类：超高（6%+）、稳健（4-6%）、金融
  - 自动识别行业分类
  - 显示前20只最佳股票

- **券商股票监控**
  - 实时获取12只主要券商股票数据
  - 显示最新价格、涨跌幅、市值
  - 分三个梯队展示

### 2. 数据缓存机制

- 数据缓存4小时，避免频繁请求
- 缓存文件：`data/cache.json`
- 自动判断缓存是否过期

### 3. 定时自动更新

- 后台定时任务每4小时自动刷新一次数据
- 无需手动操作

### 4. 手动刷新

- 点击页面右上角"刷新"按钮可立即更新数据
- 显示刷新进度和结果

## 使用方法

### 方式1：使用实时数据（推荐）

这是默认模式，系统会自动从股票市场获取最新数据。

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 确保 .env 文件中配置为实时模式
USE_REALTIME_DATA=true

# 3. 启动服务器
python backend/app.py

# 首次启动会自动加载数据，需要1-2分钟
```

### 方式2：使用静态数据

如果只需要查看预设的静态数据，可以关闭实时功能。

```bash
# 编辑 .env 文件
USE_REALTIME_DATA=false

# 启动服务器
python backend/app.py
```

## 筛选条件说明

### 高股息股票筛选标准

1. **超高股息率（6%以上）**
   - 股息率 ≥ 6%
   - 通常为周期性行业（煤炭、交通运输等）
   - 注意行业周期风险

2. **稳健高股息（4-6%）**
   - 股息率 4% - 6%
   - 非金融行业
   - 相对稳定的分红

3. **金融高股息**
   - 股息率 ≥ 4%
   - 银行、保险、证券等金融行业
   - 受政策影响较大

### 券商股票分类

- **第一梯队**：中信证券、华泰证券、国泰君安
- **第二梯队**：招商证券、广发证券、中信建投等
- **第三梯队**：申万宏源、东方财富等

## API接口

### 获取系统状态

```bash
GET /api/status

返回：
{
  "realtime_mode": true,
  "last_update_time": "2026-01-07 12:00:00",
  "cache_status": "active"
}
```

### 手动刷新数据

```bash
POST /api/refresh

返回：
{
  "success": true,
  "message": "数据刷新成功",
  "update_time": "2026-01-07 12:00:00"
}
```

## 性能优化

1. **缓存机制**
   - 首次加载后数据缓存4小时
   - 减少API请求次数
   - 加快页面加载速度

2. **异步加载**
   - 后台定时刷新不影响页面使用
   - 手动刷新显示进度提示

3. **降级策略**
   - 如果实时数据获取失败，自动回退到静态数据
   - 保证系统可用性

## 数据更新频率

- **自动更新**：每4小时
- **手动更新**：随时可点击刷新按钮
- **缓存有效期**：4小时

建议在交易日盘前（9:00前）或盘后（15:30后）手动刷新一次，获取最新数据。

## 注意事项

### 首次启动

首次启动使用实时数据模式时：
- 需要下载并处理大量股票数据
- 可能需要1-2分钟时间
- 后续使用会直接读取缓存，速度很快

### 网络要求

- 需要能够访问东方财富、新浪财经等数据源
- 建议使用稳定的网络连接
- 如果网络不稳定，可以切换到静态数据模式

### 数据准确性

- 数据来自公开市场数据源
- 仅供参考，不构成投资建议
- 重要决策请参考官方公告和专业机构分析

## 故障排除

### 问题1：数据加载失败

**原因**：网络问题或API限流

**解决方案**：
1. 检查网络连接
2. 等待几分钟后重试
3. 临时切换到静态数据模式

### 问题2：数据更新缓慢

**原因**：首次加载需要处理大量数据

**解决方案**：
- 耐心等待1-2分钟
- 后续使用会很快
- 可以查看终端日志了解进度

### 问题3：缓存文件过大

**原因**：长期使用积累的缓存数据

**解决方案**：
```bash
# 删除缓存文件
rm data/cache.json

# 重启服务器会自动重新生成
```

## 开发者说明

### 扩展筛选条件

编辑 `backend/data_fetcher.py`：

```python
# 修改股息率阈值
if dividend_rate >= 8:  # 改为8%
    stock['category'] = '超高股息率'
```

### 添加新的数据源

在 `StockDataFetcher` 类中添加新方法：

```python
def fetch_new_data_source(self):
    # 实现新的数据抓取逻辑
    pass
```

### 修改缓存时长

编辑 `.env` 文件或修改代码：

```python
self.cache_duration = timedelta(hours=2)  # 改为2小时
```

## 技术架构

```
┌─────────────────┐
│   Vue.js 前端   │
└────────┬────────┘
         │
    HTTP API
         │
┌────────▼────────┐      ┌──────────────┐
│  Flask 后端     │─────►│ AKShare API  │
│  (app.py)       │      │  (数据源)     │
└────────┬────────┘      └──────────────┘
         │
    ┌────▼─────┐
    │  缓存层   │
    │ (4小时)  │
    └──────────┘
```

## 总结

实时数据功能让你的投资跟踪系统始终展示最新的市场数据，帮助你及时发现高股息投资机会。

**重要提醒**：投资有风险，入市需谨慎！
